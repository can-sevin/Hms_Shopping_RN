import React, {Component} from 'react';
import {SafeAreaView, StyleSheet, Text, View} from 'react-native';

export default class InfoPage extends Component {
  render() {
    return (
      <SafeAreaView
        style={{
          opacity: 0.4,
          backgroundColor: '#40040404',
          flex: 1,
          alignItems: 'center',
        }}
      />
    );
  }
}

const styles = StyleSheet.create({});
